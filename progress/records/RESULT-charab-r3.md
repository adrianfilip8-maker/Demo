# RESULT-charab-r3 — blind round 3: incumbent vs SlyModel3 **stage 5**

**PROVISIONAL per C-F4.** Protocol held: fresh agent, seed-3 pairs (perch AND traversal flipped),
key committed before the spawn.

## Verdict, translated through the key

| pair | A | B | letters | by model |
|---|---|---|---|---|
| sly-closeup | base | model3 | A,A,B,A | incumbent 3 — rebuild 1 |
| sly-profile | base | model3 | B,A,B,B | rebuild 3 — incumbent 1 |
| sly-perch | model3 | base | B,B,A,B | incumbent 3 — rebuild 1 |
| traversal | model3 | base | B,B,B,neither | incumbent 3 — rebuild 0 (+1 neither) |

**Incumbent 10, rebuild 5, neither 1.** Trend: 16–0 → 9–7 → **10–5**. The rebuild REGRESSED.
Blinding held a third time: the critic's by-build total ("muted-raccoon 10, vivid-palette 5")
matches the key translation exactly, and it measured head ratios per side (~3.9 incumbent — "too
squat" — vs ~6.3 rebuild, target ~5.25).

## Why it regressed — my stage-5 fixes, scored by their own targets

- **Tail taper OVER-CORRECTED.** r2 said "uniform-diameter rope"; I strengthened the taper; r3 now
  says "narrowest at the root and widest near the tip ... a narrow tapering stick", "a third leg".
  The visible tail in posed shots is the mid/tip, so tip-weighted taper removed exactly the mass
  the silhouette needed. It also reports "hard-faceted zigzag ... concave notch on the underside" —
  which is **skinning, not the spline**: each ring is welded 100% to its nearest tail bone, so
  posed bone rotations crease the tube at every bone boundary. More spline samples cannot fix a
  skinning crease.
- **The cane grip fix produced a new artefact.** Shortening the below-hand shaft left "a second
  gold segment poking out at the hip" in perch, and the hook arc now reads as "a second confusing
  shape" on the profile silhouette. Half-fix worse than no fix.
- **The head fraction bump did not change the read.** Still "a flat grey disc", cap "a thin band
  leaving a bare tan crown" — growing HEAD_H moved the proportions but the cap dome no longer
  covers the crown, and the closeup mask still "half-misses one eye".

Two consecutive rounds of nudges have hit the ceiling of the current head construction. The
critic's own summary line: **"The amber eyes, gold set and crimson sash are already correct — the
head is the only thing keeping this build from reading as Sly."**

## The critic's three changes (all for the rebuild this round) → stage 6

1. **Head as a volume**: skull depth, wedge snout + black nose + jaw, soft crown + brim actually
   covering the top, mask over both sockets with outer points, amber eyes inside.
2. **Tail**: 2–3× thicker at root with smooth taper, continuous S (the kinks are the per-ring
   single-bone skinning — stage 6 blends each ring across two bones), ragged fur edge, clean
   perpendicular rings, re-rooted at the spine base.
3. **Compress proportions + weld the cane**: head +35–40%, kill the long pale neck (collar under
   the jaw), shorten shins to land 5–5.5 heads; cane gripped, stray segment deleted.

## Standing consequence

Unchanged — incumbent remains the default. The r2→r3 regression is recorded as the cost of
incremental nudges on a construction that needs a rebuild; stage 6 is that rebuild, not another
nudge.
