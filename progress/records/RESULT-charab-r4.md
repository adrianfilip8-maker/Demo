# RESULT-charab-r4 — blind round 4: incumbent vs SlyModel3 **stage 6**

Protocol held: fresh agent, seed-4 pairs (profile AND traversal flipped), key committed before
the spawn.

## Verdict, translated through the key

| pair | A | B | letters | by model |
|---|---|---|---|---|
| sly-closeup | base | model3 | B,A,B,B | **rebuild 3** — incumbent 1 |
| sly-profile | model3 | base | A,A,A,A | **rebuild 4** — incumbent 0 |
| sly-perch | base | model3 | B,B,B,B | **rebuild 4** — incumbent 0 |
| traversal | model3 | base | A,A,A,A | **rebuild 4** — incumbent 0 |

**Rebuild 15, incumbent 1.** Series: 16–0 → 9–7 → 10–5 → **1–15**. The critic's own by-build
tally ("vivid 15, slate 1") matches the key translation exactly, a fourth consecutive round of
verified blinding — and it flagged its own letter total as misleading before giving the by-build
one, unprompted.

## What flipped it

Stage 6 was the structural rebuild r3 demanded instead of a fourth nudge, and every one of its
mechanisms shows up in the critic's positive citations for the rebuild:

- "the ringed tail reads as a smooth banded arc" (traversal — the two-bone skinning; r3 called
  this same tail "a narrow tapering stick" and r2 "a rope")
- "the tail is carried in a thick S-curve clear of the body as in the reference" (perch)
- "amber irises inside a black mask that tapers to a point past the outer eye corner" (closeup —
  the enlarged bridge + corner spikes)
- "flat cel colour fields with a clean dark outline, matching the reference's rendering
  language" (closeup, craft)
- the costume stack "read top to bottom, exactly the costume order in the reference frame"
  (profile)

The incumbent's single point: closeup silhouette, on head-and-cap mass — the rebuild's head
"shrinks to a nub on a long thin neck" at that one camera. That is the only remaining named
deficit, and after the r3 regression the decision here is to **not** chase it: the model FREEZES
at stage 6 (`314a8c3`).

## The critic's three changes — all three target the INCUMBENT this round

Costume colour separation ("this is the whole gap"), face rebuild (white/grey eyes should be
amber; mask "a blob ... instead of a band"), contour/tail carriage. **Recorded, not applied**, as
with r2's incumbent list: the task is the new model.

## Decision posture

PREREG-charab §6's ship row (critic prefers rebuild on ≥3 of 4 questions AND G1–G5 pass) is now
satisfied on this round's numbers — fidelity and craft went 4/4 pairs to the rebuild, identity
and silhouette 3/4, and all five gates pass. **But the user's instruction reserves the decision
for the END of the 24-hour window**, and single-round margins have swung hard in this series
(10–5 → 1–15 in one stage). So:

1. **Model frozen at stage 6.** No further SlyModel3 edits before the final round — the r3
   regression is the standing warning against polishing a lead.
2. **r5 runs now on the SAME stage-6 frames, new seed, fresh critic** — an inter-critic
   reproducibility check that costs ten minutes and no render. If r5 ≈ r4, the final verdict can
   rest on one round; if they diverge, the final round becomes best-of-3.
3. **The final deciding round runs at window end** (~15:30Z 2026-08-07), per the instruction.
