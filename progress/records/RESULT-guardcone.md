# RESULT-guardcone — VOID on V-TREE: six lanes editing `src/` mid-capture split the run across three trees; the candidate is UNJUDGED and the run is re-queued

Scored against `PREREG-guardcone.md` + `AMENDMENT A1` (083c755). 49/49 frames captured, runner
exited clean, `src/` restored. Scorer log `guardcone-score-run1.log`.

## The disqualifying line

```
trees: {906574ed7908f232, fa4b2c1a3a2091e6, 2b5c7c49ad9c4668}  expected ee5ad9d3a535b155
```

Three distinct source trees across one run against a registered single-tree bar. Cause is not
mysterious: the run launched at 01:19 while **six sibling lanes were still authoring and
committing their mechanisms into the shared working tree**. V-TREE is a registered validity bar
and it is fail-closed, so **the outcome is VOID — no verdict on the cone candidate, in either
direction.** The BS1/BF1/PROT-MOON/PROT-LAMPS/PROT-B_sly-startle failures below are NOT a
refutation and must not be cited as one.

## What the run nonetheless establishes (EVIDENCE-GRADE, not verdict-grade)

The per-shot brackets held everywhere — `off` vs `back` **0 px on all sixteen shots** — so within
each shot's arm triple the render was reproducible; the tree churn did not visibly move the
frames it bracketed. On that basis, recorded for the re-run to confirm:
- **PARK1 clean in 49/49 rows** — §309's parked guard-model levers verifiably untouched on every
  arm. The owner's parking held mechanically inside a live capture.
- **BV1 PASS**: the structured beam installs as designed (shape=1, colPatrol 0xffd9a0, core 0.62,
  glow 0.42) and `interior.bon` reads `lampW` **exactly 0** — §303's underground window respected
  by arithmetic, not by hope.
- **BL1**: `bon` vs `blamp` differs on 586,503 px, **100% inside** the registered region, ΔL 16.29
  and Δ(R−B) 39.93 — the carried lamp does light the guard and its surroundings. The "it doesn't
  light the guard" half of the critic's complaint has a working mechanism.
- Open questions the re-run must answer honestly: BS1 found **zero hot-warm apex px** (the visible
  source card), BF1's blown share is 0.0000 in BOTH arms (so the bar could not discriminate — it
  may be mis-aimed), and night's MOON/LAMPS protections show 4,557/8,018 px of movement whose
  attribution is unresolved while the tree is split.

## Disposition

1. Frames archived `guardcone1-void-treechurn/`; **re-queued unchanged** (same seal, same
   amendment, same bars — §141.1: a VOID is re-run, not re-tuned) now that `src/` is clean and
   every lane has committed.
2. If the re-run reproduces BS1 = 0 and BF1's non-discriminating 0.0000/0.0000 on a single tree,
   those are then real findings: the source card is not rendering and BF1 is mis-aimed — both
   recorded here in advance so the re-run cannot be read as fishing.
